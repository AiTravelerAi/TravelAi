{
  "name": "@timetravelerai/types",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript type definitions for the TimeTravelerAI monorepo (Anchor accounts, signals, drops, pools, etc.).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint . --ext .ts",
    "typecheck": "tsc --noEmit",
    "dev": "tsc -w"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.7.4",
    "eslint": "^8.56.0",
    "prettier": "^3.3.3",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
